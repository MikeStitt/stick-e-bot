# draft9p5's results

Everything the draft reads, measures or settles, written as it happens. The plan's
§ *Where this draft's output goes* says what lands here and what does not.

- `ids.md` — the document, its workspace and its ten tabs, written by Phase A0.
- Phase A's three walks: the first reader of each variable, each sketch's plane query, and the ten
  connector names.
- Each ring's reads, one file per tab, named for the tab and the script that wrote it.

No images. The *Capture is out* gate refuses a tracked raster image under
`.docs/experiments/runs/`; a frame worth keeping goes to `.docs/experiments/build-briefs/images/`.

The register is `../register.md`, not here. This folder holds what was read; the register holds
what was decided.
